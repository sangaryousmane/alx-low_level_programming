#include <string.h>
#include "main.h"

/**
* _strcpy - copy one string to another
* @dest: destination string
* @src: source string
* Return: a pointer to those strings
*/
char *_strcpy(char *dest, char *src)
{
return (strcpy(dest, src));
}
