#include "main.h"
/**
* print_square - print square if @size is positive
* @size: param to the function
* Return nothing
*/
void print_square(int size)
{
int i, j;

if (n > 0)
{
for (i = 0 ; i < n; i++)
{
for (j = 0; j < n; j++)
{
_putchar('#');
}
_putchar('\n');
}
}
_putchar(10);
}
