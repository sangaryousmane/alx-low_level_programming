#include <stdio.h>
#include <ctype.h>


/**
* _islower - checks for lowercase character.
* _islower returns 1 if lower else 0
*/
int _islower(int c)
{
if (islower(c))
{
return (1);
}
else
{
return (0);
}
return (0);
}
