#include <stdio.h>
/**
* main - prints the name of the program
*@argc: argument counts
*@argv: actual arguments
*Return: zerror if all is succesfull
*/
int main(int __attribute__((__unused__)) argc, char *argv[])
{

printf("%s\n", argv[0]);
return (0);
}
